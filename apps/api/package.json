{
  "name": "@recruitflow/api",
  "private": true,
  "scripts": {
    "dev": "nest start --watch --path tsconfig.json --entryFile main --builder webpack",
    "start": "nest start --path tsconfig.json --entryFile main --builder webpack",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@nestjs/common": "^11.1.6",
    "@nestjs/core": "^11.1.6",
    "@nestjs/platform-express": "^11.1.6",
    "@recruitflow/config": "workspace:*",
    "@recruitflow/contracts": "workspace:*",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "stripe": "^18.1.0"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.21",
    "ts-loader": "^9.5.7"
  }
}
